# 1. Тема

**Определение прототипа, класса**

---

# 2. Главное в одну фразу

Прототип — объект, из которого JavaScript ищет унаследованные свойства, а `class` — более удобный синтаксис для работы с тем же прототипным механизмом.

---

# 3. Суть

> Прототип — объект, в котором JavaScript ищет свойство, если его нет у самого экземпляра. Поиск идёт по цепочке прототипов до `null`; собственное свойство всегда имеет приоритет.
>
> Благодаря этому сотни объектов могут пользоваться одним методом без копирования функции в каждый экземпляр. Механизм полезен и при чтении поведения объекта, которое не видно среди его собственных полей.
>
> `class` не заменяет прототипы, а даёт удобный синтаксис над ними. Методы из тела класса лежат в `Class.prototype` и разделяются экземплярами; конструктор создаёт индивидуальные поля. `extends` связывает цепочки прототипов, а `super` обращается к родительской реализации.
>
> Изменяемые массивы и объекты не стоит хранить в общем прототипе: одно состояние тогда разделят все экземпляры.

---

# 4. Самое главное запомнить

- Свойства экземпляра приоритетнее одноимённых свойств прототипа.
- Методы в прототипе не копируются в каждый экземпляр.
- `__proto__` устаревший аксессор; используйте `Object.getPrototypeOf`.
- Класс нельзя вызвать без `new`.
- Методы класса по умолчанию работают в строгом режиме.

---

# 5. Описание

```js
class Person {
  constructor(name) {
    this.name = name;
  }

  greet() {
    return `Привет, ${this.name}`;
  }
}

const anna = new Person('Анна');
Object.getPrototypeOf(anna) === Person.prototype; // true
```

| Подход | Создание метода | Наследование |
|---|---|---|
| Конструктор + prototype | `Person.prototype.greet = ...` | `Object.create()` |
| `class` | метод в теле класса | `extends` |

Не кладите изменяемые массивы и объекты в общий прототип, если состояние должно быть индивидуальным: его разделят все экземпляры. Индивидуальные поля создавайте в конструкторе или полях класса.

---

# 6. Ссылки

- [MDN: Прототипы и наследование](https://developer.mozilla.org/ru/docs/Web/JavaScript/Inheritance_and_the_prototype_chain)
- [MDN: Классы](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Classes)
