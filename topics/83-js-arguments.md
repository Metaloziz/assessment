# 1. Тема

**Псевдомассив arguments**

---

# 2. Главное в одну фразу

`arguments` — массивоподобный объект аргументов обычной функции, который в новом коде обычно заменяют rest-параметром.

---

# 3. Суть

> `arguments` — встроенный массивоподобный объект обычной функции. Он содержит все фактически переданные значения, даже если параметров в сигнатуре нет, но не является настоящим `Array`.
>
> Он встречается в старых API, обёртках и функциях с переменным числом аргументов. Важно не ожидать у него `map` или `reduce`: сначала потребуется преобразование в массив.
>
> Для нового кода используют rest-параметр `...args`. Он явно описывает переменное количество аргументов в сигнатуре и сразу даёт массив. У стрелочной функции собственного `arguments` нет: она увидит только значение из окружающей обычной функции, если оно там есть.
>
> В нестрогой простой функции элементы `arguments` могут быть связаны с именованными параметрами; rest-параметр такой неявной связи не создаёт.

---

# 4. Самое главное запомнить

- `arguments` есть только у обычных функций.
- Это не `Array`, но его можно преобразовать через `[...arguments]` или `Array.from`.
- В нестрогих простых функциях параметры и `arguments` могут быть связаны.
- Rest-параметр должен быть последним и всегда создаёт массив.

---

# 5. Описание

| Свойство | `arguments` | `...args` |
|---|---|---|
| Тип | массивоподобный объект | `Array` |
| Стрелочная функция | нет собственного | поддерживается |
| Методы массива | нет | есть |
| Видимость в сигнатуре | нет | да |

```js
function sum() {
  return [...arguments].reduce((total, value) => total + value, 0);
}

const sumModern = (...values) =>
  values.reduce((total, value) => total + value, 0);
```

`arguments` полезно знать для чтения старого кода и написания совместимых обёрток, но для новых API используйте rest-параметры.

---

# 6. Ссылки

- [MDN: arguments](https://developer.mozilla.org/ru/docs/Web/JavaScript/Reference/Functions/arguments)
- [JavaScript.info: Rest и spread](https://javascript.info/rest-parameters-spread)
