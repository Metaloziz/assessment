# 1. Тема

**Всплытие переменных**

---

# 2. Главное в одну фразу

Hoisting означает, что объявления учитываются при создании контекста до выполнения кода, но их доступность зависит от вида объявления.

---

# 3. Суть

> Hoisting — разговорное название подготовки объявлений перед выполнением кода. Строки не перемещаются: движок сначала создаёт привязки имён в области видимости, а потом идёт по инструкциям сверху вниз.
>
> Это нужно понимать при чтении кода, где функция вызывается раньше объявления, или когда неожиданно приходит `undefined` вместо ожидаемого значения. Правило помогает не маскировать ошибку порядком строк.
>
> У `var` до присваивания уже есть значение `undefined`. Function Declaration доступна вместе с телом функции. `let` и `const` тоже существуют с начала блока, но до строки инициализации находятся в temporal dead zone, поэтому чтение вызывает `ReferenceError`.
>
> Всплывает объявление, а не присваивание. Лучший способ избежать ловушек — объявлять значение до первого использования.

---

# 4. Самое главное запомнить

- У `var` всплывает объявление, а присваивание остаётся на месте.
- Function Declaration доступна целиком до объявления.
- `let` и `const` тоже создаются заранее, но TDZ запрещает чтение.
- Function Expression подчиняется правилам переменной, которой присвоена.
- Лучшее решение проблем hoisting — объявлять значение до первого использования.

---

# 5. Описание

| Код до объявления | Результат |
|---|---|
| `var x; console.log(x)` | `undefined` |
| `let x; console.log(x)` | TDZ до строки `let` |
| `function f(){}`; `f()` | работает |
| `const f = () => {}`; `f()` | TDZ до присваивания |

```js
console.log(count); // undefined
var count = 1;

// Эквивалентная модель:
var total;
console.log(total);
total = 1;
```

```js
console.log(name); // ReferenceError
const name = 'Анна';
```

Не создавайте неявные глобальные переменные присваиванием без объявления: в строгом режиме это ошибка, а в старом нестрогом коде — источник трудноуловимых багов.

---

# 6. Ссылки

- [MDN: Hoisting](https://developer.mozilla.org/ru/docs/Glossary/Hoisting)
- [JavaScript.info: Переменные](https://javascript.info/variables)
