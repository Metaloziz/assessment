# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Cursor (local rules, not shared via repo)
.cursor/rules/

# Temp
*.tmp
*.log

# Env
.env
server/.env
app/.env
app/.env.local
app/.env.*.local

# Dependencies / package managers (root has no package.json — npm/yarn here is a mistake)
node_modules/
package-lock.json
yarn.lock

# App
app/node_modules/
app/dist/

# Server
server/node_modules/
server/dist/
server/*.tsbuildinfo

