# Remote-first: `npm run dev` бьёт в Render API (CORS разрешает localhost:5173).
# Локальный server: app/.env.development.local → VITE_API_BASE_URL=http://localhost:3000
VITE_API_BASE_URL=https://assessment-api-fm0e.onrender.com
