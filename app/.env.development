# Remote-first: живые стенды бьют в Render API (CORS уже разрешает localhost:5173).
# Локальный API: создайте app/.env.local с пустым VITE_API_BASE_URL= — тогда сработает Vite proxy → :3000.
VITE_API_BASE_URL=https://assessment-api-fm0e.onrender.com
